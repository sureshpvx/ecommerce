<div class="bg-white rounded-md shadow-sm p-3">
  <% if product.images.attached? %>
    <%= image_tag product.images.first, class: "w-full h-120 object-cover rounded-t-md" %>
  <% end %>

  <h3 class="text-sm font-semibold mb-1 truncate">
    <a href="<%= product_path(product) %>" class="hover:underline">
      <%= link_to product.name, product_path(product), data: { turbo_frame: "_top" }, class: "hover:underline" %>
    </a>
  </h3>

  <p class="text-sm font-semibold text-gray-800">
    <% if product.price.present? %>
      ₹<%= product.price %>
    <% else %>
      Price: Contact Us
    <% end %>
  </p>

  <!-- Button container to align Buy Now and Add to Cart -->
  <div class="flex items-center justify-between mt-2">
    <%= button_to "🛍 Buy Now", method: :post, data: { turbo: false }, class: "bg-green-500 hover:bg-green-600 text-white text-sm font-medium py-1 px-3 rounded shadow-sm transition duration-300" %>

    <%= button_to "🛒 Add to Cart", cart_items_path(product_id: product.id), method: :post, data: { turbo: false }, class: "bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md transition duration-300 shadow-md flex items-center justify-center" %>
  </div>
</div>
